### 3. Create `.env` file
In your `renewable-readiness` root folder, create a new file called exactly `.env` (no other name) and paste this inside:
```
MONGO_URI=mongodb://localhost:27017/
MONGO_DB=renewable_readiness